apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.go-api-deployment.appName }}
spec:
  replicas: {{ .Values.go-api-deployment.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.go-api-deployment.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.go-api-deployment.appName }}
    spec:
      containers:
        - name: {{ .Values.go-api-deployment.appName }}
          image: {{ .Values.containers.goApiImage }}
          envFrom:
            - secretRef:
                name: {{ printf "%s-secret" .Values.postgres-deployment.dbName }}
          imagePullPolicy: Always   # Important: When "Never" doesn't try to pull from Docker Hub
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-service" .Values.go-api-deployment.appName }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.go-api-deployment.appName }}
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      #nodePort: 30100
