apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.postgresDeployment.dbName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.postgresDeployment.dbName }}
  template:
    metadata:
      labels:
        app: {{ .Values.postgresDeployment.dbName }}
    spec:
      containers:
        - name: {{ .Values.postgresDeployment.dbName }}
          image: {{ .Values.postgresDeployment.containers.postgresDbImage }}
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: {{ printf "%s-secret" .Values.postgresDeployment.dbName }}
          volumeMounts:
            - name: {{ printf "%s-storage" .Values.postgresDeployment.dbName }}
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: {{ printf "%s-storage" .Values.postgresDeployment.dbName }}
          persistentVolumeClaim:
            claimName: {{ printf "%s-pvc" .Values.postgresDeployment.dbName }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.postgresDeployment.dbName }}
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: {{ .Values.postgresDeployment.dbName }}
